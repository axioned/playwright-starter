{
  "name": "playwright-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:chrome": "playwright test --project=chromium",
    "test:firefox": "playwright test --project=firefox",
    "smoke": "playwright test --grep @smoke",
    "smoke:chrome": "playwright test --project=chromium --grep @smoke",
    "smoke:firefox": "playwright test --project=firefox --grep @smoke",
    "update": "npm install -D @playwright/test@latest && npx playwright install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/node": "^20.11.27"
  }
}
